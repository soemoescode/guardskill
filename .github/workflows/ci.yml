name: CI

on:
  push:
  pull_request:
  schedule:
    - cron: '0 6 * * 1'

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node-version: [18.x, 20.x, 22.x]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm test

  no-skipped-tests:
    # A test that stands down on Linux is a test nobody is running. Skips are for
    # platform limits (symlinks on unprivileged Windows), and only there.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 22.x
      - name: Fail if any test skipped itself on Linux
        run: |
          npm test 2>&1 | tee test-output.txt
          skipped=$(grep -E '^# skipped ' test-output.txt | awk '{print $3}')
          echo "skipped: ${skipped:-unknown}"
          test "${skipped}" = "0"

  self-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 22.x
      - name: GuardSkill scans its own repository
        run: node bin/guardskill.js . --fail-on high --no-color --exclude test/fixtures --exclude test/corpus
